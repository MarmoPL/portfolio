# Cloudflare Pages configuration
name = "portfolio"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Required: specify the output directory for static export
pages_build_output_dir = "out"
